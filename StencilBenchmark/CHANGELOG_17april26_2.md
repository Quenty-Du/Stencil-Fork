#### Изменения в этом коммите:
- Добавил запись времени выполнения программы в логовый файл txt (который находится в папке output), который удаляется при каждом новом запуске программы
- Переписал скрипт `build_to_debug.bat` на .bash для удобства запуска из консоли
- Добавил в .gitignore логовый файл